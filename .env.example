# Copy to .env and fill in values for local runs.
# App Platform / production should set these as real env vars instead.

DATABASE_URL=postgres://user:pass@localhost:5432/ane_safari
DATABASE_SSL=false
MODEL_ACCESS_KEY=
MODEL_NAME=o3-mini
PORT=8080
WORKER_POLL_MS=60000
BATCH_SIZE=50
